/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "standalone" removido para compatibilidade com Vercel
  // Vercel usa seu próprio sistema de build otimizado
};

module.exports = nextConfig;
